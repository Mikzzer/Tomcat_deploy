pipeline {
    agent any

    stages {
        stage('Clone repository') {
            steps {
               bat "git clone https://github.com/Mikzzer/Tomcat_deploy.git"
            }
        }
    }
}
